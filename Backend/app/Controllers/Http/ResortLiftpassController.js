'use strict'

/**
 * Resourceful controller for interacting with resortliftpasses
 */
class ResortLiftpassController {
  /**
   * Show a list of all resortliftpasses.
   * GET resortliftpasses
   */
  async index ({ request, response, view }) {
  }

  /**
   * Render a form to be used for creating a new resortliftpass.
   * GET resortliftpasses/create
   */
  async create ({ request, response, view }) {
  }

  /**
   * Create/save a new resortliftpass.
   * POST resortliftpasses
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single resortliftpass.
   * GET resortliftpasses/:id
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Render a form to update an existing resortliftpass.
   * GET resortliftpasses/:id/edit
   */
  async edit ({ params, request, response, view }) {
  }

  /**
   * Update resortliftpass details.
   * PUT or PATCH resortliftpasses/:id
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a resortliftpass with id.
   * DELETE resortliftpasses/:id
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = ResortLiftpassController
